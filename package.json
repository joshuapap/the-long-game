{
  "name": "the-long-game",
  "version": "0.1.0",
  "private": true,
  "description": "Multi-competition sports tipping platform — backend + API + thin client",
  "type": "commonjs",
  "engines": { "node": ">=22.5.0" },
  "scripts": {
    "start": "node --experimental-sqlite src/server.js",
    "seed": "node --experimental-sqlite src/db/seed.js",
    "reset": "node --experimental-sqlite src/db/seed.js --fresh",
    "test": "node --experimental-sqlite --test test/*.test.js"
  },
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "cheerio": "^1.0.0",
    "express": "^4.19.2"
  }
}
